physics:
  forward_operator: fft2(centered=False)
  backward_operator: ifft2(centered=False)
training:
  datasets:
    - name: FakeMRIBlobs
      sample_size: 4
      num_coils: 4
      spatial_shape: [4, 48, 48]
      seed: 0
      text_description: transforms
      transforms:
        cropping:
          crop: [32, 32]
          crop_type: uniform
          image_center_crop: true
        random_augmentations:
          random_rotation_degrees: [-90, 90]
          random_rotation_probability: 0.5
          random_flip_type: RANDOM
          random_flip_probability: 0.5
        sensitivity_map_estimation:
          estimate_sensitivity_maps: true
          sensitivity_maps_type: RSS_ESTIMATE
        normalization:
          scaling_key: masked_kspace
          scale_percentile: 0.99
        masking:
          name: FastMRIRandom
          accelerations: [4]
          center_fractions: [0.16]
          range_mode: discrete
          mode: static
        pad_coils: 6
        delete_kspace: true
        use_seed: false
  batch_size: 2
  optimizer: Adam
  lr: 0.001
  weight_decay: 0.0
  lr_step_size: 1000
  lr_gamma: 0.5
  lr_warmup_iter: 0
  num_iterations: 2
  validation_steps: 2
  gradient_steps: 1
  loss:
    crop: null
    losses:
      - function: l1_loss
        multiplier: 1.0
  checkpointer:
    checkpoint_steps: 100
  metrics:
    - fastmri_psnr
validation:
  datasets:
    - name: FakeMRIBlobs
      sample_size: 2
      num_coils: 4
      spatial_shape: [4, 48, 48]
      seed: 1
      text_description: transforms-val
      transforms:
        cropping:
          crop: [32, 32]
          crop_type: uniform
          image_center_crop: true
        sensitivity_map_estimation:
          estimate_sensitivity_maps: true
          sensitivity_maps_type: RSS_ESTIMATE
        normalization:
          scaling_key: masked_kspace
          scale_percentile: 0.99
        masking:
          name: FastMRIRandom
          accelerations: [4]
          center_fractions: [0.16]
          range_mode: discrete
          mode: static
        pad_coils: 6
        use_seed: true
  batch_size: 2
  crop: null
  metrics:
    - fastmri_psnr
    - fastmri_ssim
model:
  model_name: unet.unet_2d.Unet2d
  num_filters: 8
  num_pool_layers: 2
  dropout_probability: 0.0
  image_initialization: SENSE
logging:
  tensorboard:
    num_images: 2
